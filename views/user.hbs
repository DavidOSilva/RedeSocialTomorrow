<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.1/font/bootstrap-icons.css">
    <title>Profile</title>
</head>
<body>
    <nav>
        <h4>Rede Social - Tomorrow</h4>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><a href="/auth/logout">Logout</a></li>
        </ul>
    </nav>

    <div class="container py-3">
        <div class="card" style="border-radius: 15px;">
            <div class="card-body text-center">
                <div class="mt-3 mb-4">
                    <img src="/assets/user.png" class="rounded-circle img-fluid" style="width: 100px;" />
                </div>
                <h4 class="mb-2">{{user.name}} {{user.lastname}}</h4>
                <p class="text-muted mb-1">{{user.email}}</p>
                <p class="text-muted mb-4">#{{formatId user.id}}<span class="mx-2 " style="color: #a285f9; font-weight: bold;">|</span> {{posts.length}} Posts</p>
            </div>
        </div>
    </div>

    <div class="container py-3">
        {{#if posts.length}} <!-- Verifica se há posts -->
            {{#each posts}}
                <div class="card mb-3">
                    <div class="card-header">
                        <div class="row d-flex justify-content-between">
                            <div class="col-auto" style="color: #a285f9; font-weight: bold;">#{{formatId this.post_id}}</div>
                            <div class="col-auto text-muted">{{timeSince this.created_at}}</div>
                        </div>
                    </div>
                    <div class="card-body">
                        <blockquote class="blockquote mb-0">
                            <p style="text-align: justify;">
                                <i class="bi bi-quote icon-flipV" style="font-size: 1.5rem; opacity: 0.5; color:#a285f9"></i>
                                {{this.content}}
                                <i class="bi bi-quote icon-flipH" style="font-size: 1.5rem; opacity: 0.5; color:#a285f9"></i>
                            </p>
                        </blockquote>
                        <footer class="blockquote-footer d-flex justify-content-between align-items-center" style="margin: 0;">
                            <div class="flex-grow-1">{{../user.name}} {{../user.lastname}}</div>
                        </footer>
                    </div>
                </div>
            {{/each}}
        {{else}} <!-- Se não houver posts, exibe a mensagem padrão -->
            <div class="container p-3 text-center">
                <img src="/assets/no-posts.png" alt="No Posts" style="opacity: 0.2; width: 100%; max-width: 200px;">
                <h1 style="opacity: 0.8; margin-top: 10px;">Oops!</h1>
                <p style="opacity: 0.5;" class="text-muted">Nenhuma postagem encontrada.</p>
            </div>
        {{/if}}
    </div>
    
    <div style="height: 100px;"></div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>

</html>
